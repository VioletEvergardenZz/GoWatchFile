# TODO（按主线能力组织）

> 文档状态：现行（主线）
> 更新时间：2026-02-24
> 说明：本清单只保留“当前能力演进”任务，历史路线条目已归档。

## 1. P0（当前 2~4 周）

### 1.1 文件入云

- [x] 上传链路重试与队列保护策略收敛（含重试上限、饱和阈值与熔断开关）。
- [x] 上传可靠性演练脚本与复盘输出稳定（`upload-stress` / `upload-recap`）。
- [x] 补齐文件入云失败分类与人工补传场景演练样例（见 `docs/03-告警与AI/上传失败分类与人工补传演练样例.md`）。

### 1.2 告警监控与决策

- [x] 收敛告警规则模板，形成“默认规则 + 场景规则”双层结构。
- [x] 明确告警抑制与升级的可解释字段，降低值班误判成本。
- [x] 将告警处置结果与知识库推荐结果做可追溯关联（`/api/kb/recommendations` 返回 `trace`，并回写到 `/api/alerts` 的 `decision.knowledgeTrace`）。

### 1.3 AI 分析与降级

- [x] 固化 AI 回放脚本与验收手册（成功/重试/降级/失败分类）。
- [x] 接口降级响应补齐 `meta` 字段，确保失败可追溯。
- [ ] 增加 AI 分析质量基线样本的周度对比复盘。

### 1.4 知识库复用

- [x] 收敛条目生命周期状态定义并同步前后端口径（统一为 `draft/reviewing/published/archived`，并收敛 `submit/approve/reject/archive` 状态迁移规则）。
- [x] 固化问答引用率、检索命中率、评审时延的阶段门禁阈值（统一由 `/api/kb/gates` 输出，控制台与复盘脚本按同一口径执行）。
- [ ] 补齐知识导入失败与回滚场景演练。

### 1.5 统一控制台闭环

- [ ] 补齐“发现 -> 诊断 -> 回放 -> 追溯”在控制台的路径说明与验收清单。
- [ ] 完善 `/api/dashboard` 降级视图校验，避免运行态未就绪时影响值班判断。

### 1.6 支撑能力（控制面 + 系统资源 + 通知）

- [x] 统一任务状态机的超时、重试、取消口径，并补齐异常用例回归（running 超时自动重试、超限 timeout 终态、cancel/retry 规则统一、异常回归补齐）。
- [x] 增加控制面任务失败原因分布视图（接口 + 控制台）（`/api/control/tasks/failure-reasons` + 控制台 TopN 分布展示）。
- [x] 补齐 Agent 在线状态判定文档与运维巡检手册。
- [ ] 明确系统资源采集开关和权限边界（默认策略 + 风险说明）。
- [ ] 增加资源异常到告警/知识建议的联动示例。
- [x] 为关键模块补齐“接口变更必须同步文档”检查清单（见 `docs/00-文档治理/接口变更文档同步检查清单.md`）。

## 2. P1（随后 1 个季度）

- [ ] 控制面支持更稳定的分组调度策略与批量任务操作。
- [ ] 告警策略支持更清晰的分级模板与场景化预设。
- [ ] 知识库评审流和运营看板形成固定运维流程。
- [ ] 建立跨模块 SLA/SLO 报表与例行复盘节奏。

## 3. P2（中期）

- [ ] 扩展非文件类输入适配器并定义统一输入协议。
- [ ] 引入更完整的权限与审计分层机制。
- [ ] 构建跨环境发布、容量评估和故障演练标准流程。

## 4. 已明确不在当前周期内处理

- [ ] 全面微服务化拆分。
- [ ] 复杂多租户模型重构。
- [ ] 与当前业务场景无直接收益的大规模编排引擎接入。
